@php($editing = isset($dataPendukung))
<div class="grid gap-6">
<div><label class="mb-2 block text-sm font-semibold text-slate-700">Realisasi</label><select name="realisasi_id" required class="w-full rounded-xl border-slate-300 focus:border-teal-600 focus:ring-teal-600"><option value="">Pilih realisasi</option>@foreach($realisasis as $realisasi)<option value="{{ $realisasi->id }}" @selected((string)old('realisasi_id', $dataPendukung->realisasi_id ?? '') === (string)$realisasi->id)>[{{ $realisasi->tahun }} · {{ $realisasi->indikator->pilar->nama }}] {{ $realisasi->indikator->nama_indikator }}</option>@endforeach</select></div>
<div><label class="mb-2 block text-sm font-semibold text-slate-700">Judul Dokumen</label><input name="judul" value="{{ old('judul', $dataPendukung->judul ?? '') }}" required class="w-full rounded-xl border-slate-300 focus:border-teal-600 focus:ring-teal-600"></div>
<div><label class="mb-2 block text-sm font-semibold text-slate-700">File {{ $editing ? '(opsional)' : '' }}</label><input type="file" name="file" {{ $editing ? '' : 'required' }} accept=".pdf,.doc,.docx,.xls,.xlsx,.csv,.jpg,.jpeg,.png" class="w-full rounded-xl border border-slate-300 p-3 file:mr-4 file:rounded-lg file:border-0 file:bg-teal-100 file:px-4 file:py-2 file:font-semibold file:text-teal-800"><p class="mt-2 text-xs text-slate-500">Format: PDF, Office, CSV, JPG, atau PNG. Maksimal 10 MB.</p>@if($editing)<a href="{{ Storage::url($dataPendukung->file) }}" target="_blank" class="mt-2 inline-flex items-center gap-1 text-sm font-semibold text-teal-700 hover:underline"><span class="material-symbols-outlined text-lg">description</span> Lihat file saat ini</a>@endif</div>
</div><div class="mt-8 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('admin.data-pendukungs.index') }}" class="rounded-xl border border-slate-300 px-5 py-3 text-center font-semibold text-slate-700">Batal</a><button class="rounded-xl bg-teal-700 px-6 py-3 font-semibold text-white">{{ $editing ? 'Simpan Perubahan' : 'Tambah Dokumen' }}</button></div>
